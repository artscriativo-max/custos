{
  "name": "custocontrole",
  "version": "1.0.0",
  "description": "Controle de Produção e Custos - Agri Doce",
  "main": "main.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && electron .",
    "cap:init": "npm run build && npx cap init",
    "cap:add-android": "npx cap add android",
    "cap:sync": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "electron": "^28.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/camera": "^5.0.10",
    "@capacitor/cli": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/splash-screen": "^5.0.8"
  }
}
